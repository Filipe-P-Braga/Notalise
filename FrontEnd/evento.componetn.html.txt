<!-- Label do campo que será exibido na tela -->
<label for="esperarEstruturaEvento">Esperar Estrutura Evento</label>

<!-- Campo de entrada de texto -->
<input
  type="text"  <!-- Define que o campo será do tipo texto -->

  id="esperarEstruturaEvento"  
  <!-- ID do campo. Usado para ligação com o label e identificação no DOM -->

  [(ngModel)]="esperarEstruturaEvento"
  <!-- Two-way data binding do Angular.
       O valor digitado no input é armazenado na variável
       "esperarEstruturaEvento" do componente TypeScript -->

  placeholder="Digite o valor"
/>
<!-- Texto de exemplo exibido dentro do campo -->





<!-- Botão que executa o método enviar() quando clicado -->
<button (click)="enviar()">
  Enviar
</button>